[pytest]
testpaths = tests
addopts =
    -q
    --strict-markers
    --strict-config
    --cov=src/lbo
    --cov-report=term-missing
python_files = test_*.py
python_classes = Test*
python_functions = test_*
minversion = 7.0
